"""Phase 6B: single-image inference core. GUI/Qt/application state를
전혀 모른다 -- 순수하게 artifact(model JSON + state_dict + class
mapping) + 이미지 하나를 받아 예측 결과를 반환하는 계층이다(docs/
phase6a_inference_architecture.md §0/§3)."""
